CONFIG_NAME: 'stageI'

DATASET_NAME: 'birds'
EMBEDDING_TYPE: 'cnn-rnn'
GPU_ID: -1
Z_DIM: 10

TRAIN:
    FLAG: True
    PRETRAINED_MODEL: ''
    BATCH_SIZE: 2
    NUM_COPY: 2
    MAX_EPOCH: 10000
    SNAPSHOT_INTERVAL: 2000
    DISCRIMINATOR_LR: 0.002
    GENERATOR_LR: 0.002
    LR_DECAY_EPOCH: 200
    NUM_EMBEDDING: 1
    COEFF:
      KL: 2.0
    COND_AUGMENTATION: True
    B_WRONG: False

TEST:
    NUM_COPY: 10

GAN:
    EMBEDDING_DIM: 32
    DF_DIM: 64
    GF_DIM: 128
